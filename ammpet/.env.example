# Copy this file to `.env` and fill with your local values.
# Do NOT commit your real .env to version control.

# Database host (include port if needed, trailing slash preserved in properties.php defaults)
DB_HOST=localhost:3306/

# Database name
DB_NAME=dbpetshop

# Database user
DB_USER=ammphp

# Database password
DB_PASS=changeme
