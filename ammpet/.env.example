# Copy this file to `.env` and fill with your local values.
# Do NOT commit your real .env to version control.

# Database host (use host only). If you need a custom port, set DB_PORT below.
# Prefer using host without port to avoid ambiguity. Examples:
# DB_HOST=localhost
# DB_HOST=node213878-amm01.sp1.br.saveincloud.net.br
DB_HOST=localhost

# Database name
DB_NAME=dbpetshop

# Database user
DB_USER=ammphp

# Database port (optional, default 3306)
DB_PORT=3306

# Database password
DB_PASS=changeme
